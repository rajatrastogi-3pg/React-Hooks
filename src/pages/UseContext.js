import React from "react";
import Layout from "../layout";
import UseContext from "../components/ContextAPI/UseContext";

const UseContextPage = () => (
  <Layout
    heading=""
    FunctionalComponent={UseContext}
    previous="/contextAPI"
    next="/"
  />
);

export default UseContextPage;
